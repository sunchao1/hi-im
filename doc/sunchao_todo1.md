1、前置条件
1）现在是2个浏览器窗口模拟群聊发消息，其中一个浏览器窗口的用户是A窗口 A uid=100001 gateway=ws://127.0.0.1:28080/ws (?gw=1)； 窗口 B uid=100002 gateway=ws://127.0.0.1:28081/ws (?gw=2)。
A 建群后 B 在 GID 栏填入同一数字再点「加入群」（也可 ?gw=2&gid=1）。 须先点「注册并连接」，日志出现 ONLINE ok 后再操作。
假设群名叫 测试1，创建完，A先加入，B再加入。
所有的这些（群建完要落地，加入群的关系要落地）必须是持久化落地的，不是重启之后就消失了
2）发送的消息也要落地，会存储在mongodb,先不要考虑性能，现在是方便排查问题。
3）必须暂时重启服务以后可以立马看到效果不希望有什么镜像缓存导致的修复了也不起作用
4）make m6-heal-down stop 所有服务，以及demo-web + ONLINE probe。
5）我认为如果没有任何代码修改，是不需要进行 m6-heal-down && make m6-heal 的，如果有任何代码修改，我再手动执行 m6-heal-down && make m6-heal
2、做完前置条件以后就开始模拟验证问题
    1）A窗口uid=100001 发5条消息是 100001+1，100001+2，100001+3，100001+4，100001+5
    2）B窗口uid=100002 发5条消息是 100002+1，100002+2，100002+3，100002+4，100002+5
    3）然后只追踪A窗口和B窗口发消息所应该走的任何服务节点，按先后顺序，在任何节点进行单独手工测试，由我来测试，你告诉我预期是什么，执行方法是什么，怎么操作，全程由我手动操作，
    出现问题，我们一起排查，没有问题就把todo的对应项，我来标注执行完成。
    4）你要设计这样的追踪修复计划，叫"跨Gateway群聊丢消息追踪修复计划.md"。 按我的理解可能就是对任意服务节点可执行类似接口入参，出参，你会在文档里标注预期的入参，出参是什么，如果有问题我们一起修复。当然我的理解不够全面，你来掌握。
    5）我的理解是，理论上讲，如果这次的所有节点的手动测试都在预期之内，就绝对不会出现跨Gateway群聊丢消息的情况。

